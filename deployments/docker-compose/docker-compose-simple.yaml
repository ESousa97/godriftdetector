version: '3.8'

services:
  myapp:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    environment:
      APP_NAME: myapp
      DB_PASSWORD: secret_123_xyz
      API_TOKEN: token_abc_def
      LOG_LEVEL: info
