version: '3.8'

services:
  webserver:
    image: nginx:alpine
    ports:
      - "80:80"
    environment:
      NGINX_HOST: localhost
      LOG_LEVEL: info

  cache:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    environment:
      REDIS_PORT: 6379
